"""
Subtitle Generator Package Setup.

This is a minimal setup.py for backward compatibility with older pip versions.
All configuration is now in pyproject.toml (PEP 621).
"""

from setuptools import setup

# This file exists for backward compatibility with older pip versions
# that don't fully support PEP 517/518/621.
# All package configuration is in pyproject.toml.

if __name__ == "__main__":
    setup()
